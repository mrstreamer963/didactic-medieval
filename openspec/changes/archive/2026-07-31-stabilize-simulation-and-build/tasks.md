## 1. Сборка и документация

- [x] 1.1 Обновить `build:wasm` и связанные dev-команды так, чтобы стандартная сборка не требовала интерактивной установки `wasm-opt`.
- [x] 1.2 Добавить отдельную документированную команду для оптимизированной WASM-сборки при наличии `wasm-opt`.
- [x] 1.3 Обновить `README.md` и `devlog.md`: заменить `crates/core` на `crates/game_core`, описать актуальный `Path` и координаты в tile-units.
- [x] 1.4 Привести затронутые основные OpenSpec-спеки к валидному формату заголовков, не изменяя архивные артефакты.

## 2. Пауза и игровой цикл

- [x] 2.1 Убрать вызов `world.tick(0)` из pause-ветки фронтендового ticker.
- [x] 2.2 Сохранить отображение последнего состояния карты, объектов, юнитов и статусных иконок во время паузы.
- [x] 2.3 Добавить тест или проверяемый harness, подтверждающий отсутствие simulation tick при `gameSpeed === 0`.

## 3. Карта и строительство

- [x] 3.1 Ввести единую проверку границ, проходимости и занятости клетки перед созданием `ConstructionSite` и `ConstructionJob`.
- [x] 3.2 Применить проверку ко всем типам: wall, bed и berrybush.
- [x] 3.3 Гарантировать, что завершение berrybush не меняет проходимость клетки, а завершение wall блокирует её.
- [x] 3.4 Добавить Rust-тесты на заблокированные клетки для всех трёх типов и на повторное размещение в занятой клетке.

## 4. Начальная генерация мира

- [x] 4.1 Реализовать выбор трёх различных проходимых клеток для начальных кустов.
- [x] 4.2 Добавить тест на ровно три `FoodSource(BerryBush, 5)` при нескольких фиксированных seed, включая seed с потенциальными коллизиями.
- [x] 4.3 Согласовать выбранное число стартовых юнитов между фронтендом, документацией и спецификациями.

## 5. Потребности и маршрутизация

- [x] 5.1 Передавать позицию нуждающегося юнита в поиск ближайшего berrybush/bed и выбирать объект по минимальному расстоянию от юнита.
- [x] 5.2 Добавить Rust-тесты, где два объекта расположены так, чтобы выбор относительно начала координат отличался от выбора относительно юнита.
- [x] 5.3 Создавать начальный маршрут только через A* к достижимой проходимой цели.
- [x] 5.4 Удалить прямые fallback-маршруты, способные пересекать заблокированные клетки.
- [x] 5.5 Добавить тесты на недостижимую цель и на отсутствие пересечения заблокированных клеток при движении.

## 6. Проверка результата

- [x] 6.1 Запустить `cargo fmt --check`, `cargo test --workspace` и устранить ошибки clippy в затронутом коде.
- [x] 6.2 Запустить `npm test`, TypeScript-проверку и стандартный production build.
- [x] 6.3 Проверить вручную в браузере: пауза, строительство на blocked tile, строительство куста, потребности и регенерация мира.
- [x] 6.4 Убедиться, что `git diff` не содержит изменений под `openspec/changes/archive/`.
